version: 0.2

phases:
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - rm -rf node_modules/
artifacts:
  base-directory: '.'
  files:
    - '**/*'
